# sash
I was bored and made a shell.

## How to compile manually:
1. Clone the repository: `git clone https://github.com/sams331/sash`
2. go to the cloned directory: `cd sash`
3. compile it: `gcc sash.c -o sash -lreadline`

but honestly you can just use the binary
